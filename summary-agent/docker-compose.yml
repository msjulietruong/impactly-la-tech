version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: test-summary-agent
    ports:
      - "8000:8000"
    command: >
      poetry run uvicorn api.app:app
      --host 0.0.0.0
      --port 8000
      --reload
    restart: unless-stopped
