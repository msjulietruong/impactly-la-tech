info:
  name: search products by name
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:3001/api/search/chocolate?limit=9
  params:
    - name: limit
      value: "9"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
